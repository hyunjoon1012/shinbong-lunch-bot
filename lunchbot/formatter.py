"""
============================================================
 Student B 담당 파일 — 급식 데이터를 읽기 좋은 메시지로 만들기
============================================================

■ 이 파일에서 할 일
    급식 데이터(dict)  →  사람이 읽기 좋은 문자열(str)

■ 실행해서 확인하는 방법
    터미널에서:  python -m lunchbot.formatter
    (VS Code 오른쪽 위 ▶ 버튼을 눌러도 됩니다)

  ★ 이 파일은 인터넷이 없어도 됩니다. 아래에 연습용 데이터가 들어 있어서
    Student A 의 코드가 아직 없어도 혼자 만들 수 있습니다.

■ AI(ChatGPT 등)에게 물어보는 방법
    1) 이 파일 전체를 복사한다  (Ctrl+A → Ctrl+C, Mac은 Cmd+A → Cmd+C)
    2) AI 채팅창에 붙여넣는다
    3) 아래 문장을 이어서 보낸다

    ┌──── 복사해서 쓰는 질문 ────────────────────────┐
    │ 이건 고등학교 1학년 파이썬 프로젝트 파일이야.  │
    │ 파일 아래쪽 테스트를 통과하도록                │
    │ format_meal 함수의 TODO 부분만 채워줘.         │
    │                                                │
    │ 규칙:                                          │
    │ - class, async 를 쓰지 마                      │
    │ - 새 라이브러리를 추가하지 마                  │
    │ - if / for / list / dict / f-string 만 사용해  │
    │ - 주석은 한국어로 써줘                         │
    │ - 코드를 한 줄씩 왜 그렇게 썼는지 설명해줘     │
    │ - 파일 전체를 다시 쓰지 말고 TODO 부분만 줘    │
    │ - 아래 테스트 코드는 고치지 마                 │
    └────────────────────────────────────────────────┘

    ※ AI가 준 코드를 붙여넣기 전에 설명을 먼저 읽으세요.
      발표 때 "이 줄은 무슨 일을 하나요?" 라는 질문을 받습니다.
"""


# =========================================================
#  1. 어떤 데이터가 들어오나요?
# =========================================================
#
#  Student A 가 만드는 get_meal() 함수가 이런 dict 를 넘겨줍니다.
#
#      {
#          "date": "2026-08-27",
#          "menu": ["현미밥", "김치찌개", "치킨까스", "깍두기"],
#          "calories": "735 Kcal"
#      }
#
#  급식이 없는 날에는 menu 가 빈 리스트로 들어옵니다. (팀에서 정한 약속)
#
#      {"date": "2026-08-23", "menu": [], "calories": ""}


# =========================================================
#  2. 어떤 문자열을 만들어야 하나요?
# =========================================================
#
#  ● 급식이 있는 날 (\n 은 줄바꿈이라는 뜻입니다)
#
#      🍚 8월 27일 신봉고 급식
#
#      현미밥
#      김치찌개
#      치킨까스
#      깍두기
#
#      735 Kcal
#
#  ● 급식이 없는 날
#
#      오늘은 급식이 없습니다.


# =========================================================
#  3. 내가 만들어야 하는 함수
# =========================================================

def format_meal(meal: dict) -> str:
    """
    급식 데이터를 사람이 읽기 좋은 문자열로 바꾼다.

    Example input:
        {"date": "2026-08-27",
         "menu": ["현미밥", "김치찌개"],
         "calories": "735 Kcal"}

    Example output:
        "🍚 8월 27일 신봉고 급식\n\n현미밥\n김치찌개\n\n735 Kcal"

    급식이 없는 날 (menu 가 빈 리스트):
        "오늘은 급식이 없습니다."
    """
    # TODO: Student B
    #
    #  1. 먼저 급식이 없는 경우를 처리한다.
    #     if not meal["menu"]:  →  "오늘은 급식이 없습니다." 를 return
    #
    #  2. 날짜를 보기 좋게 바꾼다.
    #     "2026-08-27"  →  "8월 27일"
    #     힌트: meal["date"].split("-") 을 하면 ["2026", "08", "27"] 이 된다.
    #     힌트: int("08") 을 하면 8 이 된다.
    #
    #  3. 메뉴들을 한 줄에 하나씩 이어붙인다.
    #     힌트 1: for 문으로 하나씩 더하기
    #     힌트 2: "\n".join(meal["menu"]) 를 쓰면 한 줄로 끝난다
    #
    #  4. 제목 + 빈 줄 + 메뉴 + 빈 줄 + 칼로리 를 합쳐서 return 한다.
    #     힌트: f-string 을 쓰면 편하다.  f"🍚 {월}월 {일}일 신봉고 급식"
    #
    raise NotImplementedError


# =========================================================
#  4. 내 코드 테스트하기
#
#     터미널에서:  python -m lunchbot.formatter
#
#     ✅ 가 전부 나오면 성공입니다.
#     이 아래는 고치지 마세요.
# =========================================================

def check(설명, 조건):
    if 조건:
        print("  ✅", 설명)
    else:
        print("  ❌", 설명)


# 연습용 데이터 (인터넷 없이 테스트하기 위한 것)
샘플_급식 = {
    "date": "2026-08-27",
    "menu": ["현미밥", "김치찌개", "치킨까스", "깍두기"],
    "calories": "735 Kcal",
}

샘플_급식없음 = {
    "date": "2026-08-23",
    "menu": [],
    "calories": "",
}


if __name__ == "__main__":
    try:
        print("=== 1. 급식이 있는 날 ===")
        메시지 = format_meal(샘플_급식)
        print("--- 만들어진 메시지 ---")
        print(메시지)
        print("----------------------")
        check("문자열(str)을 돌려준다", type(메시지) is str)
        check("'현미밥' 이 들어있다", "현미밥" in 메시지)
        check("'김치찌개' 가 들어있다", "김치찌개" in 메시지)
        check("'치킨까스' 가 들어있다", "치킨까스" in 메시지)
        check("'깍두기' 가 들어있다", "깍두기" in 메시지)
        check("'8월 27일' 이 들어있다", "8월 27일" in 메시지)
        check("칼로리가 들어있다", "735 Kcal" in 메시지)
        check("줄바꿈이 들어있다", "\n" in 메시지)

        print()
        print("=== 2. 급식이 없는 날 ===")
        메시지2 = format_meal(샘플_급식없음)
        print("--- 만들어진 메시지 ---")
        print(메시지2)
        print("----------------------")
        check("문자열(str)을 돌려준다", type(메시지2) is str)
        check("'급식이 없습니다' 가 들어있다", "급식이 없습니다" in 메시지2)

        print()
        print("테스트 끝. ❌ 가 있으면 그 부분을 고쳐보세요.")

    except NotImplementedError:
        print("  ⏳ 아직 구현하지 않았습니다. 위의 TODO 부분을 채워주세요.")
        print("     (지금 이 메시지가 나오는 것은 정상입니다)")
